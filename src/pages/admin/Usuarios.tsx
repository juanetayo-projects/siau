import Proximamente from '../../components/Proximamente'

export default function Usuarios() {
  return <Proximamente titulo="Usuarios" subtitulo="Gestión de roles y módulos por usuario (tabla consola_perfiles unificada)" />
}
